---
name: repomix-reference-2026-fwdays-agentic-large-day02-hw
description: Reference codebase for 2026 Fwdays Agentic Large Day02 Hw. Use this skill when you need to understand the structure, implementation patterns, or code details of the 2026 Fwdays Agentic Large Day02 Hw project.
---

# 2026 Fwdays Agentic Large Day02 Hw Codebase Reference

933 files | 246502 lines | 3412334 tokens

## Overview

Use this skill when you need to:
- Understand project structure and file organization
- Find where specific functionality is implemented
- Read source code for any file
- Search for code patterns or keywords

## Files

| File | Contents |
|------|----------|
| `references/summary.md` | **Start here** - Purpose, format explanation, and statistics |
| `references/project-structure.md` | Directory tree with line counts per file |
| `references/files.md` | All file contents (search with `## File: <path>`) |
| `references/tech-stack.md` | Languages, frameworks, and dependencies |

## How to Use

### 1. Find file locations

Check `project-structure.md` for the directory tree:

```text
src/
  index.ts (42 lines)
  utils/
    helpers.ts (128 lines)
```

### 2. Read file contents

Grep in `files.md` for the file path:

```text
## File: src/utils/helpers.ts
```

### 3. Search for code

Grep in `files.md` for keywords:

```text
function calculateTotal
```

## Common Use Cases

**Understand a feature:**
1. Search `project-structure.md` for related file names
2. Read the main implementation file in `files.md`
3. Search for imports/references to trace dependencies

**Debug an error:**
1. Grep the error message or class name in `files.md`
2. Check line counts in `project-structure.md` to find large files

**Find all usages:**
1. Grep function or variable name in `files.md`

## Tips

- Use line counts in `project-structure.md` to estimate file complexity
- Search `## File:` pattern to jump between files
- Check `summary.md` for excluded files, format details, and file statistics
- Check `tech-stack.md` for languages, frameworks, and dependencies

## Excalidraw-Specific Navigation

**Core Packages to Explore:**
- `packages/excalidraw/` — Main editor component and UI
- `excalidraw-app/` — Web application wrapper
- `packages/element/` — Element manipulation and geometry
- `packages/math/` — Math utilities for rendering
- `packages/common/` — Shared utilities and constants

**Key Entry Points:**
- Grep `packages/excalidraw/index.tsx` in `files.md` for main component
- Grep `excalidraw-app/App.tsx` for web app entry
- Grep `packages/excalidraw/index.tsx` for public API exports

**Search for Core Patterns:**
- `actionManager` — State management system
- `executeAction` — Method to invoke actions/state changes
- `AppState` — Main application state type
- `ExcalidrawElement` — Element schema and types
- `Renderer.ts` — Canvas 2D rendering (protected file)
- `manager.tsx` — Action system (protected file)
- `exportToSvg` — SVG export utility
- `Canvas2D`, `canvas.getContext('2d')` — Rendering context

**Examples of Common Searches:**
- Find where actions are defined: grep `type.*Action.*=' in files.md'`
- Locate element utilities: grep `packages/element/src/` in project-structure.md
- Track rendering: grep `Renderer\|renderScene\|canvas` in files.md
- Explore UI components: grep `packages/excalidraw/components/` in project-structure.md

---

This skill was generated by [Repomix](https://github.com/yamadashy/repomix)
